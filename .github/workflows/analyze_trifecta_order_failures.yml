name: Analyze trifecta order failures

on:
  push:
    branches:
      - experiment/popular-head-skip-v01
    paths:
      - 'scripts/analyze_trifecta_order_failures.py'
      - '.github/workflows/analyze_trifecta_order_failures.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: experiment/popular-head-skip-v01
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas
      - run: python scripts/analyze_trifecta_order_failures.py
      - name: Commit analysis
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add keirin_data/strategy_context/trifecta_order_failure_cases.csv \
                  keirin_data/strategy_context/trifecta_order_failure_summary.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Add trifecta order failure diagnosis'
          git pull --rebase origin experiment/popular-head-skip-v01
          git push origin HEAD:experiment/popular-head-skip-v01
